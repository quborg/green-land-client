import { FilterListRounded, ViewColumnSharp } from '@material-ui/icons';

const components: TYPES.TableComponents = {
  icons: {
    ViewColumnIcon: ViewColumnSharp,
    FilterIcon: FilterListRounded,
  },
};

export default components;
